# ==========================================================
# Variáveis de ambiente — Grupo FLM (copie para .env.local)
# ==========================================================

# URL canônica pública do site (usada em metadata, sitemap, robots, Open Graph).
NEXT_PUBLIC_SITE_URL="https://www.grupoflm.com.br"

# Endpoint que receberá os leads do formulário (integração futura).
LEAD_WEBHOOK_URL=""

# Verificação de propriedade do Google Search Console (opcional).
NEXT_PUBLIC_GOOGLE_SITE_VERIFICATION=""
